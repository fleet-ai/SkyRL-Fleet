"""Negotiation environment package: dependency-free game logic (game.py),
prompt construction (prompts.py), scenario loading (scenarios.py), the eval
harness (eval/), and the RLVR training environment (env.py)."""
