# ARC-AGI-3 B7: GRPO RL using arc-witness-agent (v5 SFT warm-start)
#
# Differences vs witness-grpo-rules-grid.yaml (v3 GRPO):
#   - Workdir: SkyRL-Fleet (current) — same branch guanghan/sft-b6
#   - GAME_IDS: 5 high-pass-rate games (tw02 tw03 tw05 tw07 tw13) per
#     v5 SFT pass-rate ranking. Phase 2 will expand to 13.
#   - env.env_class=witness_agent (NEW env class wrapping arc-witness-agent
#     instead of the v3 light-harness)
#   - SFT_CHECKPOINT_S3 downloaded as starting policy weights
#   - Clones arc-witness-agent into ~/arc-witness-agent for env_agent.py
#     to import
#   - REWARD_MODE: not used by env_agent — agent uses geometric+outcome
#     reward computed in agent/runtime/process_reward.py
#
# Usage:
#   cd SkyRL-Fleet
#   sky launch tasks/witness-grpo-v5b7.yaml \
#     --env WANDB_API_KEY=<key> \
#     --env AWS_ACCESS_KEY_ID=<key> \
#     --env AWS_SECRET_ACCESS_KEY=<key> \
#     -y --down
#
# Reference design: artifacts/arc-witness-runs/reports/2026-05-09_b7_rl_training_design.md

resources:
  disk_size: 750
  ports: 6479
  any_of:
    - accelerators: H200:8
      cloud: gcp
      use_spot: true
      image_id: projects/deeplearning-platform-release/global/images/common-cu128-ubuntu-2204-nvidia-570-v20260305
    - accelerators: H200:8
      cloud: runpod
    - accelerators: H200:8
      cloud: lambda
    - accelerators: H200:8
      cloud: nebius
    - accelerators: H200-SXM:8
      cloud: vast

num_nodes: 1

workdir:
  url: https://github.com/fleet-ai/SkyRL-Fleet.git
  ref: guanghan/sft-b6

# file_mounts: rsync local repo trees to the cluster. Avoids GitHub auth
# (arc-witness-agent is private company asset). Source dirs respect .skyignore
# (we exclude .venv, traces, logs, memory_store/runtime data, .git).
# Local working tree (incl. uncommitted changes) is what gets used on cluster
# — same pattern as the v5 SFT yaml mounting sft_pairs_judged.jsonl.
file_mounts:
  ~/arc-witness-agent: ~/Documents/obsidian/research-directions/repos/arc-witness-agent
  ~/arc-witness-envs: ~/Documents/obsidian/research-directions/repos/arc-witness-envs

envs:
  LOGGER: "wandb"
  INFERENCE_BACKEND: "vllm"
  NUM_EPOCHS: 25
  # B7 Phase 1 — 8 train games (multiple of train_batch_size=4 → no drop_last loss).
  # Selection: medium/high SFT pass rate + diverse modes (5 cell + 3 dot).
  GAME_IDS: "tw03 tw06 tw02 tw05 tw08 tw11 tw13 tw07"
  # 4 held-out games (also multiple of 4 → clean eval batching).
  # tw04 dropped (Finding K — broken multi-color cursor perception, no signal).
  VAL_GAME_IDS: "tw01 tw09 tw10 tw12"
  MAX_LEVELS: "5"
  MAX_ORAI_STEPS: "30"
  REWARD_MODE: "shaped"   # ignored by env_agent (uses geometric+outcome internally)
  OBS_MODE: "grid"        # ignored by env_agent
  RULES_MODE: "rules_unknown"
  MAX_TURNS: "30"         # max env.step() ticks ≈ max_orai_steps
  # SFT-warm-start: load v5 SFT-merged checkpoint as starting policy.
  SFT_CHECKPOINT_S3: "s3://fleet-guanghan/witness_sft_v5/merged/"
  # SkyRL/Fleet's default trajectory upload target (skyrl-trajectories) is a
  # team bucket the user IAM (fleet-guanghan) doesn't have write access to.
  # Redirect dumped rollouts to user's own bucket. SkyRL writes under
  # rollouts/{run_name}/global_step_N.jsonl, so final path is
  # s3://fleet-guanghan/rollouts/witness_grpo_v5b7/global_step_*.jsonl.
  S3_TRAJECTORY_BUCKET: "fleet-guanghan"
  MODEL: "Qwen/Qwen3.5-9B"  # base model — overridden by SFT ckpt at load

setup: |
  # --- Python environment & base deps (same as v3 GRPO) ---
  if ! command -v c++ &>/dev/null; then
    sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends build-essential
  fi
  uv venv --python 3.12 --seed
  source .venv/bin/activate
  uv sync --extra fsdp
  for f in .venv/bin/ray .venv/lib/python*/site-packages/ray/core/src/ray/raylet/raylet; do
    [ -f "$f" ] && chmod +x "$f" 2>/dev/null || true
  done
  uv pip install wandb boto3 awscli pyyaml

  # --- Qwen3.5 deps ---
  source scripts/fleet-qwen35-extra-setup.sh

  # --- arcengine SDK ---
  uv pip install arc-agi

  # --- arc-witness-envs + arc-witness-agent are file_mounted ---
  # See top of yaml `file_mounts` section. Local repo trees (with uncommitted
  # changes) rsynced to cluster, so no GitHub auth needed.
  export WITNESS_ENVS_DIR=$HOME/arc-witness-envs
  export ARC_WITNESS_AGENT_DIR=$HOME/arc-witness-agent

  # Install agent's runtime deps (lightweight — agent uses transformers/torch
  # already in the venv, plus pyyaml for config loading).
  uv pip install pyyaml openai

  # Sanity check the file_mounts arrived as expected.
  test -f "$ARC_WITNESS_AGENT_DIR/agent/core.py" || { echo "ERROR: arc-witness-agent file_mount missing"; exit 1; }
  test -f "$WITNESS_ENVS_DIR/witness_grid.py" || { echo "ERROR: arc-witness-envs file_mount missing"; exit 1; }
  echo "[ok] file_mounts: arc-witness-agent + arc-witness-envs both present"

  # --- Download v5 SFT checkpoint from S3 ---
  mkdir -p $HOME/sft_ckpt
  aws s3 sync "$SFT_CHECKPOINT_S3" "$HOME/sft_ckpt/" 2>&1 | tail -5

  # --- Patch missing multimodal config files from base HF model ---
  # SkyRL's save_hf_model only writes weights + tokenizer + config.json,
  # but vLLM detects Qwen3.5-9B as multimodal and tries to load
  # preprocessor_config.json (image processor) from the same dir. Without
  # this, AsyncVLLMInferenceEngine.__init__ raises OSError. We download
  # the missing multimodal config files from HF and copy them into sft_ckpt.
  echo "[patch] downloading multimodal config from Qwen/Qwen3.5-9B → ~/sft_ckpt"
  python -c "from huggingface_hub import snapshot_download; snapshot_download(repo_id='Qwen/Qwen3.5-9B', allow_patterns=['preprocessor_config.json','processor_config.json','chat_template.json','chat_template.jinja','video_preprocessor_config.json'], local_dir='$HOME/sft_ckpt', local_dir_use_symlinks=False)"
  echo "[patch] sft_ckpt contents after patch:"
  ls -la "$HOME/sft_ckpt/" | head -25

  # --- Prepare dataset (env_class=witness_agent for B7) ---
  cd ~/sky_workdir
  python examples/train_integrations/witness/prepare_witness_dataset.py \
    --game_ids $GAME_IDS \
    --val_game_ids $VAL_GAME_IDS \
    --reward_mode $REWARD_MODE \
    --obs_mode $OBS_MODE \
    --rules_mode $RULES_MODE \
    --max_levels $MAX_LEVELS \
    --env_class witness_agent \
    --max_orai_steps $MAX_ORAI_STEPS \
    --output_dir $HOME/data/witness_v5b7

run: |
  set -euo pipefail

  source .venv/bin/activate
  [ -f "$HOME/.cuda_env" ] && source "$HOME/.cuda_env"

  TMP_DIR="$HOME/skyrl-tmp"
  mkdir -p "$TMP_DIR"
  export TMPDIR="$TMP_DIR"
  export WITNESS_ENVS_DIR=$HOME/arc-witness-envs
  export ARC_WITNESS_AGENT_DIR=$HOME/arc-witness-agent
  export HYDRA_FULL_ERROR=1

  # --- GCP NCCL/gIB handling (identical to v3 GRPO) ---
  ON_GCP=false
  if [ -d "/usr/local/gib" ] || { [ -f "/sys/class/dmi/id/product_name" ] && grep -qi "google" /sys/class/dmi/id/product_name 2>/dev/null; }; then
    ON_GCP=true
  fi
  if [ "$ON_GCP" = true ]; then
    if ! { [ -d "/sys/class/infiniband" ] && [ "$(ls /sys/class/infiniband/ 2>/dev/null)" ]; }; then
      echo "GCP without RDMA — disabling gIB"
      export LD_LIBRARY_PATH=$(echo "${LD_LIBRARY_PATH:-}" | sed 's|/usr/local/gib/lib64:||g; s|:/usr/local/gib/lib64||g; s|/usr/local/gib/lib64||g')
      unset NCCL_NET 2>/dev/null || true
      unset NCCL_CROSS_NIC NCCL_NET_GDR_LEVEL NCCL_P2P_NET_CHUNKSIZE NCCL_NVLS_CHUNKSIZE 2>/dev/null || true
      unset NCCL_IB_ADAPTIVE_ROUTING NCCL_IB_QPS_PER_CONNECTION NCCL_IB_TC NCCL_IB_FIFO_TC 2>/dev/null || true
      unset NCCL_TUNER_CONFIG_PATH 2>/dev/null || true
      export NCCL_CUMEM_ENABLE=0
    fi
    SHM_SIZE=$(df --output=size /dev/shm 2>/dev/null | tail -1 | tr -d ' ')
    if [ -n "$SHM_SIZE" ] && [ "$SHM_SIZE" -lt 16777216 ]; then
      sudo mount -o remount,size=16G /dev/shm 2>&1 || true
    fi
  fi
  sudo sysctl -w kernel.yama.ptrace_scope=0 2>/dev/null || true
  ulimit -n 1048576 2>/dev/null || ulimit -n "$(ulimit -Hn)" 2>/dev/null || true

  # --- Ray setup ---
  export RAY_RUNTIME_ENV_HOOK=ray._private.runtime_env.uv_runtime_env_hook.hook
  export RAY_object_store_memory=10000000000
  export RAY_DISABLE_MEMORY_MONITOR=1

  read -r head_ip _ <<< "$SKYPILOT_NODE_IPS"
  DATA_DIR="$HOME/data/witness_v5b7"

  python3 -c "import wandb; wandb.login(relogin=True, key='$WANDB_API_KEY')"

  wait_for_ray() {
    local address=$1
    for _ in $(seq 1 24); do
      if ray status --address "$address" >/dev/null 2>&1; then
        return 0
      fi
      sleep 5
    done
    echo "Ray cluster at $address failed to become ready" >&2
    return 1
  }

  if [ "${SKYPILOT_NODE_RANK:-0}" = "0" ]; then
    if ! ray status --address 127.0.0.1:6479 >/dev/null 2>&1; then
      ray start --head --disable-usage-stats --port 6479 --object-store-memory=10000000000
    fi
    wait_for_ray 127.0.0.1:6479

    TOTAL_POLICY_GPUS=$((SKYPILOT_NUM_GPUS_PER_NODE * ${SKYPILOT_NUM_NODES:-1}))

    # Use SFT checkpoint as starting policy weights, NOT base Qwen3.5-9B.
    # This is the v5 SFT warm-start point.
    POLICY_PATH="$HOME/sft_ckpt"

    # --- Live log persistence: stream stdout/stderr to file + S3 every 30s ---
    # Two clusters previously died at ~5min after wandb init (Ray crash) with
    # no captured logs because --down auto-cleaned. This guarantees we get
    # full trainer output in S3 even if Ray/cluster dies.
    S3_LOG_BASE="s3://fleet-guanghan/witness_grpo_v5b7/logs"
    LOG_DIR="$HOME/run_logs"
    mkdir -p "$LOG_DIR"
    TRAIN_LOG="$LOG_DIR/trainer.log"
    DIAG_LOG="$LOG_DIR/diagnostics.log"

    # Pre-train diagnostic dump (so we have a snapshot even if trainer crashes immediately)
    {
      echo "=== Pre-train diagnostics $(date -u +%FT%TZ) ==="
      echo "--- nvidia-smi ---"
      nvidia-smi || echo "nvidia-smi failed"
      echo "--- free -h ---"; free -h
      echo "--- df -h ---";   df -h | head -20
      echo "--- POLICY_PATH ($POLICY_PATH) contents ---"
      ls -la "$POLICY_PATH" 2>&1 | head -30
      du -sh "$POLICY_PATH" 2>&1
      echo "--- ray status ---"
      ray status --address 127.0.0.1:6479 2>&1 | head -40
      echo "--- python/torch/vllm versions ---"
      python -c "import torch, vllm, ray; print(f'torch={torch.__version__} cuda={torch.cuda.is_available()} ngpu={torch.cuda.device_count()} vllm={vllm.__version__} ray={ray.__version__}')" 2>&1
    } > "$DIAG_LOG" 2>&1
    aws s3 cp "$DIAG_LOG" "$S3_LOG_BASE/diagnostics.log" 2>&1 | tail -3 || true

    # Background S3 mirror loop — copies trainer.log every 30s while training runs.
    (
      while true; do
        if [ -f "$TRAIN_LOG" ]; then
          aws s3 cp "$TRAIN_LOG" "$S3_LOG_BASE/trainer.log" --quiet 2>/dev/null || true
        fi
        sleep 30
      done
    ) &
    LOG_MIRROR_PID=$!

    # Ensure final flush + cleanup mirror loop on exit (success OR crash).
    trap '
      kill $LOG_MIRROR_PID 2>/dev/null || true
      [ -f "$TRAIN_LOG" ] && aws s3 cp "$TRAIN_LOG" "$S3_LOG_BASE/trainer.log" 2>&1 | tail -3 || true
      echo "[trap] final log uploaded to $S3_LOG_BASE/trainer.log"
    ' EXIT

    # `unbuffer` (or python -u) keeps Python from buffering stdout.
    # Without this, the last few seconds of output before crash would be in
    # process buffer and never flushed.
    # Disable -e/-pipefail around the trainer so the failure-handler block below
    # runs even when trainer crashes (yaml top-level set -euo pipefail would
    # otherwise short-circuit).
    set +e
    set +o pipefail
    PYTHONUNBUFFERED=1 python -u -m examples.train_integrations.witness.entrypoints.main_witness \
      data.train_data="['${DATA_DIR}/train.parquet']" \
      data.val_data="['${DATA_DIR}/validation.parquet']" \
      trainer.algorithm.advantage_estimator="grpo" \
      trainer.policy.model.path="$POLICY_PATH" \
      trainer.flash_attn=false \
      generator.chat_template_kwargs='{enable_thinking:false}' \
      trainer.loss_chunk_size=4096 \
      trainer.use_sample_packing=false \
      trainer.algorithm.loss_reduction="sequence_mean" \
      trainer.placement.colocate_all=true \
      trainer.strategy=fsdp2 \
      trainer.placement.policy_num_gpus_per_node="$SKYPILOT_NUM_GPUS_PER_NODE" \
      trainer.placement.ref_num_gpus_per_node="$SKYPILOT_NUM_GPUS_PER_NODE" \
      trainer.placement.ref_num_nodes="${SKYPILOT_NUM_NODES:-1}" \
      trainer.placement.policy_num_nodes="${SKYPILOT_NUM_NODES:-1}" \
      generator.num_inference_engines="$TOTAL_POLICY_GPUS" \
      generator.inference_engine_tensor_parallel_size=1 \
      trainer.epochs=${NUM_EPOCHS} \
      trainer.eval_batch_size=4 \
      trainer.eval_before_train=true \
      trainer.eval_interval=5 \
      trainer.update_epochs_per_batch=1 \
      trainer.train_batch_size=4 \
      trainer.policy_mini_batch_size=4 \
      trainer.micro_forward_batch_size_per_gpu=1 \
      trainer.micro_train_batch_size_per_gpu=1 \
      trainer.ckpt_interval=10 \
      trainer.max_prompt_length=4096 \
      generator.max_input_length=16384 \
      generator.sampling_params.max_generate_length=1024 \
      generator.sampling_params.temperature=0.7 \
      generator.sampling_params.top_p=0.95 \
      generator.eval_sampling_params.max_generate_length=1024 \
      trainer.policy.optimizer_config.lr=1.0e-6 \
      trainer.algorithm.use_kl_loss=true \
      trainer.algorithm.kl_loss_coef=0.001 \
      generator.max_turns=$MAX_TURNS \
      generator.backend="$INFERENCE_BACKEND" \
      generator.run_engines_locally=true \
      generator.weight_sync_backend=nccl \
      generator.async_engine=true \
      generator.batched=false \
      environment.env_class=witness_agent \
      generator.use_conversation_multi_turn=true \
      generator.n_samples_per_prompt=4 \
      generator.eval_n_samples_per_prompt=3 \
      generator.gpu_memory_utilization=0.8 \
      trainer.logger="$LOGGER" \
      trainer.project_name="arc-agi-3" \
      trainer.run_name="witness_grpo_v5b7" \
      trainer.resume_mode=null \
      trainer.ckpt_path="$HOME/ckpts/witness_grpo_v5b7" \
      trainer.dump_data_batch=true \
      trainer.dump_training_trajectories=true \
      2>&1 | tee "$TRAIN_LOG"
    PYTHON_EXIT=${PIPESTATUS[0]}
    echo "[trainer exit] code=$PYTHON_EXIT"
    # If trainer crashed, surface the bottom of trainer.log to make sky logs useful.
    if [ "$PYTHON_EXIT" != "0" ]; then
      echo "=== TAIL OF TRAINER LOG (last 100 lines) ==="
      tail -100 "$TRAIN_LOG" || true
      echo "=== END TAIL ==="
    fi
    # Re-enable strict mode for the rest of the run section.
    set -eo pipefail
  else
    if ! ray status --address "$head_ip:6479" >/dev/null 2>&1; then
      ray start --address "$head_ip:6479" --disable-usage-stats
    fi
    wait_for_ray "$head_ip:6479"
    sleep infinity
  fi

  # --- Sync ckpts to S3 (best effort) ---
  set +e
  S3_BASE="s3://fleet-guanghan/witness_grpo_v5b7"
  echo "[s3] uploading run logs + checkpoints to ${S3_BASE}"
  aws s3 sync "$HOME/ckpts/witness_grpo_v5b7" "${S3_BASE}/" 2>&1 | tail -5
  echo "[done] B7 RL complete. Artifacts at ${S3_BASE}/"
