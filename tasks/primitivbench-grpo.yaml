# PrimitivBench mini-flywheel: GRPO training, arms B / C (D-15 three-arm protocol)
#
# Adapted from tasks/witness-grpo.yaml — recipe overrides are kept IDENTICAL except:
# entrypoint (main_primitivbench), train/val data (mixed parquets), run_name, trainer.seed.
#
# Usage (arm B, then arm C with the SAME TRAIN_SEED — paired-seed design):
#   cd SkyRL-Fleet
#   ARM=B sky launch tasks/primitivbench-grpo.yaml \
#     --env FLEET_API_KEY=<key> --env WANDB_API_KEY=<key> \
#     --env AWS_ACCESS_KEY_ID=<key> --env AWS_SECRET_ACCESS_KEY=<key> \
#     --env ARM=B -y --down
#   ... then the same command with --env ARM=C
#
# PRE-FLIGHT (see examples/train_integrations/primitivbench/README.md):
#   1. MODEL and GAME_IDS must match the reusable arm-A baseline run.
#      WARNING: the witness default GAME_IDS (tw10 tw09 tw13 tw04) overlaps the
#      pre-registered held-out list (tw01/09/10/12). Resolve before launch.
#   2. Push the current branch and update workdir.ref below.
#   3. Keep TRAIN_SEED identical across arms B and C.

resources:
  disk_size: 750
  ports: 6479
  any_of:
    - accelerators: H200:8
      cloud: gcp
      use_spot: true
      image_id: projects/deeplearning-platform-release/global/images/common-cu128-ubuntu-2204-nvidia-570-v20260305
    - accelerators: H200:8
      cloud: runpod
    - accelerators: H200:8
      cloud: lambda
    - accelerators: H200:8
      cloud: nebius
    - accelerators: H200-SXM:8
      cloud: vast

num_nodes: 1

workdir:
  url: https://github.com/fleet-ai/SkyRL-v2.git
  ref: guanghan/b7-phase2-judge-rewards   # update to the branch carrying primitivbench/

envs:
  LOGGER: "wandb"
  INFERENCE_BACKEND: "vllm"
  NUM_EPOCHS: 500
  ARM: "B"                     # B = top-12 portfolio | C = placebo-12 active control
  TRAIN_SEED: "42"             # keep IDENTICAL across arms (paired-seed design, D-15)
  # Witness base corpus — MUST match the reusable arm-A baseline run
  GAME_IDS: "tw10 tw09 tw13 tw04"
  MAX_LEVELS: "5"
  REWARD_MODE: "shaped"
  OBS_MODE: "grid"
  RULES_MODE: "rules_given"
  MAX_TURNS: "50"
  PB_MAX_TURNS: "30"
  PB_SEEDS_PER_GAME: "64"
  MODEL: "Qwen/Qwen3.5-9B"     # must match arm A's student (9B vs 35B-A3B: decide once)

setup: |
  # --- Python environment & base deps (identical to witness-grpo.yaml) ---
  if ! command -v c++ &>/dev/null; then
    sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends build-essential
  fi
  uv venv --python 3.12 --seed
  source .venv/bin/activate
  uv sync --extra fsdp
  for f in .venv/bin/ray .venv/lib/python*/site-packages/ray/core/src/ray/raylet/raylet; do
    [ -f "$f" ] && chmod +x "$f" 2>/dev/null || true
  done
  uv pip install wandb boto3 awscli

  # --- Qwen3.5 deps ---
  source scripts/fleet-qwen35-extra-setup.sh

  # --- arcengine SDK ---
  uv pip install arc-agi

  # --- arc-witness-envs (game definitions for the witness base corpus) ---
  if [ ! -d "$HOME/arc-witness-envs" ]; then
    git clone https://github.com/fleet-guanghan/arc-witness-envs.git $HOME/arc-witness-envs
  fi
  export WITNESS_ENVS_DIR=$HOME/arc-witness-envs

  # --- 1. witness base dataset (same prep as the baseline arm) ---
  cd ~/sky_workdir
  python examples/train_integrations/witness/prepare_witness_dataset.py \
    --game_ids $GAME_IDS \
    --reward_mode $REWARD_MODE \
    --obs_mode $OBS_MODE \
    --rules_mode $RULES_MODE \
    --max_levels $MAX_LEVELS \
    --output_dir $HOME/data/witness

  # --- 2. PrimitivBench portfolio dataset + merge (games ship pre-vendored in repo) ---
  if [ "$ARM" = "B" ]; then
    PORTFOLIO=examples/train_integrations/primitivbench/portfolios/portfolio_v1.json
  else
    PORTFOLIO=examples/train_integrations/primitivbench/portfolios/portfolio_placebo_v1.json
  fi
  python examples/train_integrations/primitivbench/prepare_primitivbench_dataset.py \
    --portfolio $PORTFOLIO \
    --seeds-per-game $PB_SEEDS_PER_GAME \
    --max-turns $PB_MAX_TURNS \
    --output $HOME/data/primitivbench/pb_train.parquet \
    --val-output $HOME/data/primitivbench/pb_val.parquet \
    --merge-witness $HOME/data/witness/train.parquet \
    --merged-output $HOME/data/primitivbench/arm${ARM}_mixed.parquet

run: |
  set -euo pipefail

  source .venv/bin/activate
  [ -f "$HOME/.cuda_env" ] && source "$HOME/.cuda_env"

  TMP_DIR="$HOME/skyrl-tmp"
  mkdir -p "$TMP_DIR"
  export TMPDIR="$TMP_DIR"
  export WITNESS_ENVS_DIR=$HOME/arc-witness-envs
  export PRIMITIVBENCH_GAMES_DIR=$HOME/sky_workdir/examples/train_integrations/primitivbench/games
  export HYDRA_FULL_ERROR=1

  # --- GCP NCCL/gIB handling (from fleet-common-run.sh) ---
  ON_GCP=false
  if [ -d "/usr/local/gib" ] || { [ -f "/sys/class/dmi/id/product_name" ] && grep -qi "google" /sys/class/dmi/id/product_name 2>/dev/null; }; then
    ON_GCP=true
  fi
  if [ "$ON_GCP" = true ]; then
    if ! { [ -d "/sys/class/infiniband" ] && [ "$(ls /sys/class/infiniband/ 2>/dev/null)" ]; }; then
      echo "GCP without RDMA — disabling gIB"
      export LD_LIBRARY_PATH=$(echo "${LD_LIBRARY_PATH:-}" | sed 's|/usr/local/gib/lib64:||g; s|:/usr/local/gib/lib64||g; s|/usr/local/gib/lib64||g')
      unset NCCL_NET 2>/dev/null || true
      unset NCCL_CROSS_NIC NCCL_NET_GDR_LEVEL NCCL_P2P_NET_CHUNKSIZE NCCL_NVLS_CHUNKSIZE 2>/dev/null || true
      unset NCCL_IB_ADAPTIVE_ROUTING NCCL_IB_QPS_PER_CONNECTION NCCL_IB_TC NCCL_IB_FIFO_TC 2>/dev/null || true
      unset NCCL_TUNER_CONFIG_PATH 2>/dev/null || true
      export NCCL_CUMEM_ENABLE=0
    fi
    SHM_SIZE=$(df --output=size /dev/shm 2>/dev/null | tail -1 | tr -d ' ')
    if [ -n "$SHM_SIZE" ] && [ "$SHM_SIZE" -lt 16777216 ]; then
      sudo mount -o remount,size=16G /dev/shm 2>&1 || true
    fi
  fi
  sudo sysctl -w kernel.yama.ptrace_scope=0 2>/dev/null || true
  ulimit -n 1048576 2>/dev/null || ulimit -n "$(ulimit -Hn)" 2>/dev/null || true

  # --- Ray setup ---
  export RAY_RUNTIME_ENV_HOOK=ray._private.runtime_env.uv_runtime_env_hook.hook
  export RAY_object_store_memory=10000000000
  export RAY_DISABLE_MEMORY_MONITOR=1

  read -r head_ip _ <<< "$SKYPILOT_NODE_IPS"
  DATA_DIR="$HOME/data/witness"
  PB_DATA_DIR="$HOME/data/primitivbench"

  python3 -c "import wandb; wandb.login(relogin=True, key='$WANDB_API_KEY')"

  wait_for_ray() {
    local address=$1
    for _ in $(seq 1 24); do
      if ray status --address "$address" >/dev/null 2>&1; then
        return 0
      fi
      sleep 5
    done
    echo "Ray cluster at $address failed to become ready" >&2
    return 1
  }

  if [ "${SKYPILOT_NODE_RANK:-0}" = "0" ]; then
    if ! ray status --address 127.0.0.1:6479 >/dev/null 2>&1; then
      ray start --head --disable-usage-stats --port 6479 --object-store-memory=10000000000
    fi
    wait_for_ray 127.0.0.1:6479

    TOTAL_POLICY_GPUS=$((SKYPILOT_NUM_GPUS_PER_NODE * ${SKYPILOT_NUM_NODES:-1}))

    python -m examples.train_integrations.primitivbench.entrypoints.main_primitivbench \
      data.train_data="['${PB_DATA_DIR}/arm${ARM}_mixed.parquet']" \
      data.val_data="['${DATA_DIR}/validation.parquet','${PB_DATA_DIR}/pb_val.parquet']" \
      trainer.seed=${TRAIN_SEED} \
      trainer.algorithm.advantage_estimator="grpo" \
      trainer.policy.model.path="$MODEL" \
      trainer.flash_attn=false \
      generator.chat_template_kwargs='{enable_thinking:true}' \
      trainer.loss_chunk_size=4096 \
      trainer.use_sample_packing=false \
      trainer.algorithm.loss_reduction="sequence_mean" \
      trainer.placement.colocate_all=true \
      trainer.strategy=fsdp2 \
      trainer.placement.policy_num_gpus_per_node="$SKYPILOT_NUM_GPUS_PER_NODE" \
      trainer.placement.ref_num_gpus_per_node="$SKYPILOT_NUM_GPUS_PER_NODE" \
      trainer.placement.ref_num_nodes="${SKYPILOT_NUM_NODES:-1}" \
      trainer.placement.policy_num_nodes="${SKYPILOT_NUM_NODES:-1}" \
      generator.num_inference_engines="$TOTAL_POLICY_GPUS" \
      generator.inference_engine_tensor_parallel_size=1 \
      trainer.epochs=${NUM_EPOCHS} \
      trainer.eval_batch_size=4 \
      trainer.eval_before_train=true \
      trainer.eval_interval=5 \
      trainer.update_epochs_per_batch=1 \
      trainer.train_batch_size=4 \
      trainer.policy_mini_batch_size=4 \
      trainer.micro_forward_batch_size_per_gpu=1 \
      trainer.micro_train_batch_size_per_gpu=1 \
      trainer.ckpt_interval=10 \
      trainer.max_prompt_length=2048 \
      generator.max_input_length=16384 \
      generator.sampling_params.max_generate_length=1024 \
      generator.sampling_params.temperature=0.6 \
      generator.sampling_params.top_p=0.95 \
      generator.sampling_params.stop='["</action>"]' \
      generator.eval_sampling_params.stop='["</action>"]' \
      generator.eval_sampling_params.max_generate_length=1024 \
      trainer.policy.optimizer_config.lr=1.0e-6 \
      trainer.algorithm.use_kl_loss=true \
      generator.max_turns=$MAX_TURNS \
      generator.backend="$INFERENCE_BACKEND" \
      generator.run_engines_locally=true \
      generator.weight_sync_backend=nccl \
      generator.async_engine=true \
      generator.batched=false \
      environment.env_class=witness \
      generator.use_conversation_multi_turn=true \
      generator.n_samples_per_prompt=4 \
      generator.eval_n_samples_per_prompt=3 \
      generator.gpu_memory_utilization=0.8 \
      trainer.logger="$LOGGER" \
      trainer.project_name="arc-agi-3" \
      trainer.run_name="primitivbench_arm${ARM}_seed${TRAIN_SEED}" \
      trainer.resume_mode=null \
      trainer.ckpt_path="$HOME/ckpts/primitivbench_arm${ARM}_ckpt" \
      trainer.dump_data_batch=true \
      trainer.dump_training_trajectories=true
  else
    if ! ray status --address "$head_ip:6479" >/dev/null 2>&1; then
      ray start --address "$head_ip:6479" --disable-usage-stats
    fi
    wait_for_ray "$head_ip:6479"
    sleep infinity
  fi
